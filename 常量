常量
1.使用const修饰
2.常量在定义的时候就必须初始化
3.常量不可以修改
4.常量只能修饰bool，数值类型（int，float），string类型

const{
	a=iota
	b
	c
}//表示给a赋值为0，b在a的基础上+1，c在b的基础上+1，这个是逐行递增的
const{
	a=iota
	b=iota
	c,d=iota
}//输出为，a=0,b=1,c=2,d=2


5.没有常量名必须字母大写的规范
6.仍然可以通过首字母的大小写来控制常量的访问范围
